Wants strict step-by-step workflow for complex/error-prone setup tasks: only 1 step per message, wait for their feedback/output before giving the next step; avoid assumptions/options until data is provided.

ADR 2026-02-18 (Accepted): Audit zero-asumsi & konsistensi skema untuk Laravel 12 (non-blade). Prinsip: keputusan wajib berbasis Migrasi → Model → Kode; bila konflik, controller/service yang menyesuaikan. Qty/stok harus INT UNSIGNED; hapus float/ROUND non-blade (rg kosong). Profit/COGS stabil pakai snapshot: sales_invoice_lines.total_modal & service_order_lines.total_modal (khusus servis SELESAI) agar tidak berubah saat modal_per_unit item berubah. Expenses ikut migrasi: status POSTED/VOID (bukan deleted_at); void pakai void_at + alasan_void. Gunakan action controllers (Index/Create/Store/Show/Void) agar tipis & mudah diaudit (boleh >100 LOC jika kritikal). Work remaining: Kasir servis E2E; today history (auth/filter); admin master & operasional (Items, Technicians, Reminders, PayrollRuns, TechnicianLoans); AuthController wiring/policy registration evidence.

Wants controllers kept thin (ideally <100 LOC) with a hexagonal-style separation; prefers splitting per action (e.g., Index/Create/Store/Show/Void controllers) to make audits easy.

Requires auditing to be zero-assumption: decisions must be based strictly on project vision + migrations + models + existing code evidence; no 'try/guess/tambal'; if something unclear, verify with targeted searches (rg/sed) before prescribing changes; every code change must have explicit rationale tied to the above. Audit must be specific and based on three main pillars: project vision → DB migrations → Eloquent models → existing code evidence. No 'try/guess/tambal'. Every change must have a rationale connected to these three pillars.

Tambahkan aturan kerja: untuk hal kritikal (wiring/kontrak publik/flow security), jangan pakai “kira-kira”; keputusan harus berbasis data repo + keputusan blueprint. Jika belum pasti, verifikasi dulu (mis. rg) sebelum memberi langkah spesifik.

Meminta: untuk hal kritikal (wiring/kontrak publik/flow security), jangan pakai “kira-kira”; keputusan harus berbasis data repo + keputusan blueprint; jika belum pasti, verifikasi dulu (mis. rg) sebelum memberi langkah spesifik.

Tambahkan aturan: untuk hal kritikal (wiring/kontrak publik/flow security), jangan pakai “kira-kira”; keputusan harus berbasis data repo + keputusan blueprint. Jika lokasi/konfigurasi belum pasti, lakukan verifikasi dulu dengan pencarian (mis. rg) dan baru beri langkah yang spesifik.

Wants audits done step-by-step (one step at a time), only give next steps after they report results; alternatively, if doing an audit, wait for complete reported data before proposing solutions; also wants routes cleaned/organized. User also wants guidance/debugging in strict step-by-step mode: provide only one next step at a time and wait for their result before continuing.

2026-02-12 audit snapshot: user ran `make audit` + `go test ./...` + component tests; semuanya PASS (unit+component, vet, boundary/package checks, docs_audit, testtags_audit, content_audit). Ripgrep map untuk auth security menunjukkan lokasi kunci: internal/config/{auth_load.go(auth CSRFCookieName,CookieSameSite,AllowedOrigins),auth_validate.go,auth_types.go}; internal/modules/auth/usecase/{refresh.go (generate new CSRF), google_callback_issue.go}; internal/modules/auth/transport/http/{google_cookie.go(setAuthCookies+parseSameSite), session_cookie.go, session_handler*.go + component tests}; internal/platform/token/jwt/types.go (Sid claim); internal/transport/http/server/server.go (CORS config allow origins + X-CSRF-Token header); trust middleware: origin.go(RequireOrigin), csrf.go(RequireCSRFWithCode), https_only.go(RequireHTTPS), rate_limit.go; router: internal/transport/http/router/v1/auth/{policy.go (mwAuthCookieStrict uses RequireHTTPS/Origin/CSRF), policy_state.go, routes.go POST /auth/refresh}, v1/router.go (RequireHTTPS applied to auth/protected, protected uses middleware.JWTAuth), v1/me/routes.go reads SessionID.

Simpan “10 step sampai public-ready” sebagai cetak biru progres project: (1) skeleton MVP E2E, (2) auth Google OIDC + refresh rotation, (3) model data inti, (4) upload pipeline + SQS, (5) worker deploy engine + ZIP security + quota, (6) edge routing CloudFront Function + KVS, (7) dashboard minimal, (8) observability + audit trail, (9) abuse & safety baseline, (10) launch checklist (domain/docs/runbook/alarms).

Keputusan MVP yang disepakati (opsi A semuanya): DB = DynamoDB; edge routing = CloudFront Function + KVS untuk mapping host→site_id→current_release_id + suspended; storage quota 20MB = hard fail deploy jika hasil extract melebihi limit; pageview quota (HTML) = soft warning + grace period sebelum suspend; auth browser = dashboard & API di 1 eTLD+1 (mis. app.domain.com & api.domain.com) dengan refresh token rotation via HttpOnly cookie + mitigasi CSRF.

Saat meminta keputusan/clarification, AI wajib memberi beberapa opsi jawaban disertai plus-minus dan rekomendasi yang sesuai kondisi saat ini (blueprint, standar enterprise, dan konteks repo), bukan bertanya secara “kosong”.

Current product focus is AWS: use CloudFront + S3 (other providers in the repo are for preparation, not currently in use and should be marked/inactive).

Has an enterprise blueprint for a static site hosting platform via ZIP: Google login OIDC, event-driven serverless AWS architecture (Go API in Lambda + Function URL/API Gateway, SQS, worker Lambda with upgrade path to Fargate), S3 uploads + immutable releases, DynamoDB for metadata & usage, CloudFront + Function/KVS for mapping host→site_id→current_release_id & suspended, rollback via pointer to current_release_id, quota of 20MB extract per site + pageview HTML counted in batches from CloudFront logs, ZIP security (zip slip/bomb, symlink rejection, size/file/depth limits, timeout), Jakarta region (ap-southeast-3), and a phased roadmap from MVP → quota → subdomain → custom domain → reseller → dynamic. This blueprint should be treated as a long-term architectural reference for future discussions.

Provided AI_RULES for working on a Go (Echo) repo with strict architecture boundaries, protected public contracts, error handling via presenter.HTTPErrorHandler, file/package hygiene, debug routes gating, JSONB policy, required snapshots before blueprint, and Definition of Done commands/sanity checks; user wants these rules remembered and applied flexibly. User also defined strict AI_PROMPT and AI_TUNING for the repo, including hard architectural boundaries, protected contracts, snapshot-before-change requirement, Definition of Done, and workflow expectations. The rules include: public contracts protected (router.Register, v1.Register, presenter.HTTPErrorHandler), 1 folder=1 package, file <=100 lines (if >100, must #TODO justify, but files >100 lines are allowed if critical and justifiable), debug endpoints prohibited unless requested and must be gated with DEBUG_ROUTES=1, all error responses via presenter.HTTPErrorHandler (no stacktrace/vendor/SQL/token; logging must redact Authorization/Cookie/ApiKey/token), JSONB only internal and not sent raw (allowlist/redact), strict hexagonal boundaries (domain/ports/usecase/transport/platform with import restrictions; enforced via scripts/audit_boundaries.sh & make audit), router/presenter contract, security baseline (no secrets, refresh token hashed + rotation anti-reuse + revoke, rate limiting TBD before public), Definition of Done (file list+full content+gofmt/test/vet/make audit+curl sanity), Required Snapshot before blueprint, and workflow: summarize requirements → critique risks → blueprint → then execute. The repo structure includes: cmd/{api,migrate,worker}, docs/{adr,core,internal/ai,...}, internal/{app,config,modules(account,audit,auth,domains,hosting,system,trust),platform(datastore,edge,google,objectstore,queue,state,token),security,shared,transport/http(middleware,presenter,router(v1,v2),server)}, migrations/, scripts/, Makefile/make/*.

Wants backup files (bootstrap.sh.bak.*) deleted later once everything is finished.

Bapak rekan kantor yang dimaksud user berumur 37 tahun.

(Asyraf Mubarak) is 22 years old.
